# Declare the configuration values for the AWS Resources created
#Increment the counter for when you want to force the pipeline
#Counter: 21
DEPLOY_RESOURCES: 'false'
DELETE_INFRASTRUCTURE: 'true' 
ENABLE_VPN: 'true'
AWS_DEFAULT_REGION: 'us-east-1'
